// 009 Prompt takes whatever value we type into a prompt dialog

prompt("How are you?");

// We can store a prompt in a variable:

var a = prompt("Do you like TV");

// We can print out the value stored in the variable as explained in snippet _005:

console.log(a);